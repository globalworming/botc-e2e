package com.headissue.botc_unofficial.e2e.actor
/*
import net.serenitybdd.screenplay.Actor

fun Actor.role():ActorRole=if(abilityTo(UseAMobileDevice::class.java)!=null){
  ActorRole.MobileActor
}else{
  ActorRole.ApiActor
}

/*
actor.assumingTheyCan(BrowseTheWeb.class).attemptsTo(
  Open.url(url), Click.on(otherTarget)
)

actor.assumingTheyCan(CallRestApi.class).attemptsTo(
  Post(..)
)
 */