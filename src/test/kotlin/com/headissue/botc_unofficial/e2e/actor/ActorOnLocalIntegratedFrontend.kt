package com.headissue.botc_unofficial.e2e.actor

/**
 * FYI, can't extend actor with kotlin
 *
 * "Accidental override: The following declarations have the same JVM signature..."
 *
 * see https://stackoverflow.com/a/51950911/1359903
 */
class ActorOnLocalIntegratedFrontend(name: String) { // : Actor(name) {
}